import { createUseStyles } from 'react-jss';

const useStyles = createUseStyles({
  mainContainer: {
    display: 'flex',
    alignItems: 'center',
  },
  smMargin: {
    margin: '2px',
  },
  actionDiv: {
    textAlign: 'center',
  },
})

export { useStyles }